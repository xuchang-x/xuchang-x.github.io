---
title: 异步调用
date: 2022-03-28 20:36:41
tags:
    - 异步调用
---

// 所有Future成功时返回多个Future返回值构成的List对象
Futures.allAsList(responseFuture);
// 同上，但对未成功的会用null进行替代
Futures.successfulAsList(responseFuture).get(timeout,TimeUnit.MILLISECONDS);